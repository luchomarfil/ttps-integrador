	<div class="field">
		<%= f.label t( :type ) %><br>
		<%= (f.select :type, types.map { |ta| [t(ta.to_s.downcase),ta.name] }) unless f.object.persisted? %>
		<%= t(f.object.class.name.downcase) if f.object.persisted? %>
	</div>
	<div class="field">
		<%= f.label t( :value ) %><br>
		<%= f.text_field :value %>
		<%= f.link_to_remove t( "crud.remove_contact" ) %>
	</div>
